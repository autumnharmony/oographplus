package ru.ssau.graphplus;

import com.google.common.collect.Lists;
import ru.ssau.graphplus.api.DiagramModel;
import ru.ssau.graphplus.api.Node;
//import ru.ssau.graphplus.node.ClientNode;
//import ru.ssau.graphplus.node.ServerNode;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

/**
 * Unit test for simple App.
 */
public class AppTest 

{

    @org.junit.Test
    public void testName(){
//        DiagramModel diagramModel = mock(DiagramModel.class);
//        DiagramCodeGenerator diagramCodeGenerator = new DiagramCodeGenerator();
//        when(diagramModel.getNodes()).thenReturn(Lists.<Node>asList((Node)new ServerNode("a"), (Node[]) new ClientNode[]{new ClientNode("b")}));
//        String code = diagramCodeGenerator.generateCode(new DiagramCodeSource(diagramModel));

    }
}
